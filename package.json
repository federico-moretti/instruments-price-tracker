{
  "name": "thomann-price-check",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node index.js",
    "check:console": "NOTIFY_PROVIDER=console node --env-file=.env index.js",
    "check:telegram": "NOTIFY_PROVIDER=telegram node --env-file=.env index.js",
    "check:discord": "NOTIFY_PROVIDER=discord node --env-file=.env index.js"
  },
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
